
spring.application.name=cwslife

# Backend runs on this port
server.port=8080

# MySQL connection
#spring.datasource.url=jdbc:mysql://localhost:3306/cwslife_db
#spring.datasource.username=root
#spring.datasource.password=Root@123


# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}

# Driver
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

#  EMAIL CONFIGURATION 
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dipalicws@gmail.com
spring.mail.password=vyzi dinq kyrn yvpl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Frontend URL reset link 
app.frontend.url=https://app-lifeqa-fe.azurewebsites.net

# File upload settings
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Upload folder path
app.upload.dir=${UPLOAD_DIR:/home/site/wwwroot/uploads/events}


# ACTUATOR SETTINGS
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always
